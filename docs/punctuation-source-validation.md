# 标点来源与字幕轨道对照

日期：2026-09-01；视频：X627czLUsGY（Say These 100 DAILY WORDS in a British Accent! (MODERN RP)）。

## 已证实的结论

**显式请求 en-GB 已成功取回带标点字幕。** 旧主按钮只使用保存的 en，没有参考网页上的英国英语人工轨；这份 en 响应没有句末标点。不能把它扩大为“网站没有标点”或“Supadata 会删除标点”。更不能因为 availableLangs 只列 en 就断言 en-GB 不可获取。

| 实际请求 / 证据 | 结果 |
| --- | --- |
| YouTube 公开轨道元数据 | a.en / en / asr；.en-GB / en-GB / 人工 |
| 官方 Playground，Native、带时间，Auto | 439 条，lang=en，availableLangs=[en]；0 条含英文句号/问号/感叹号 |
| 官方 Playground，相同条件，显式 en | 与 Auto 完整 JSON 相同，439 条无句末标点 |
| 官方 API，显式 en-GB、native、text=false、无 chunkSize | HTTP 200，275 条，其中237条含英文句末标点；仍返回 lang=en、availableLangs=[en] |
| 当前生产解析/分组运行实际 en-GB 响应 | 275 条 → 105 组，原成员对象、顺序、时间不变 |
| 当前生产 UI + 真实响应回放 + 模拟 Chrome Port | 105 组 / 2页，原始视图275条 / 3页；可见标点，不再全片合一 |

本次 en-GB 首条 offset=40、duration=5840，保留句末标点和换行。最终原条目起点1237400、终点1261720毫秒。请求区域语言但响应语言为 en，说明仅靠返回语言标签不足以判断是否取错轨；没有取得 YouTube 人工轨正文做逐字哈希，仍不宣称两者完全一致。

## 修复与验证

- 默认按保存语言寻找网页公布的同语言人工轨：en 可选择 en-GB；显式区域偏好 en-US 不自动替换成 en-GB。没有匹配轨时沿用已保存设置，不硬编码本视频语言。
- 新增“Supadata 请求字幕”选择，覆盖仅限本次请求。选择不会调用服务或修改保存的语言/Key；切换显示也不消耗额度。服务只接受语言偏好，不能保证区分同语言代码的多条轨道。
- 后台校验本次语言，返回 requestedLanguage；内容桥接与面板保留请求/返回语言。标签不同显示说明，不自动再请求或误报必然回退。
- 32/32 单元、17/17 生产脚本模拟集成、类型检查和构建通过。新增测试包含区域语言、恶意语言参数、响应语言归一、原双语正文及换行保留；这些测试数据为合成数据。
- 真实数据的 UI 点击第1/53/105组分别发出原 cueId 0/119/274，对应40/597680/1237400毫秒。这里只验证发送绑定与数据；没有真实视频播放器，不能当作音画准确性通过。
- 400×900视口，无横向溢出，error/warn为空。模拟视频切换清空旧行和播放提示，新会话默认选择正确。UI回放没有真实API请求。

## 双语边界

用户要求网站有现成双语就直接取用，不新增翻译。本项目解析/显示保留返回的完整文本和换行，不丢弃其中中文。当前视频公开元数据只有两条英语轨，实际 en / en-GB 响应均为英文；不能承诺已取回双语或把两次单语请求说成一次双语请求。官方 API 只提供单个 lang 偏好，未提供获取浏览器扩展渲染结果的参数。

页面曾加载沉浸式翻译字幕脚本，菜单有 Bilingual 后缀；未逐字证明后缀生成路径。用户已关闭该插件并明确要求不再调查，立即停止相关排查。不读取该插件存储，不调用其服务，不重新翻译。

## 真实验证边界

- 两次早期 Auto/en 请求日志各1 credit，合计2 credits。后续一次 en-GB 请求成功，其计费日志未独立核对；不把预计费用当实测费用。
- en-GB 请求使用官方控制台可见的生成代码与官方请求参数完成。生成代码曾在工具输出中包含凭据，之后未再输出；未写入项目、测试夹具或交付包，交付记录不得复制凭据。无配置变更。
- Playground下拉框没列en-GB，不等于API拒绝该参数。之前“未执行en-GB”的结论已被此次实际请求更新。
- 独立HTTP对YouTube timedtext的200/0字节及网页转写loading是历史失败证据，不重复探测，不把它当作当前Supadata链路阻塞。
- 已安装扩展重载、真实前中后点击准确性、真实视频切换/多标签页仍待用户验收。工具不能操作扩展侧栏和管理页，未绕过限制；构建不是安装生效。

## 本地证据

- 去凭据实际响应：C:/Users/alxanday/AppData/Local/Temp/ylh-supadata-native-en-gb-live-20260901.json。
- 早期Auto/en响应：同目录 ylh-supadata-native-live-20260901.json、ylh-supadata-native-en-live-20260901.json；两者SHA256相同，为475C875BF5FE632F3DD955AB9B68FCE1EAC5EBAF6EA5786947E93D56DF2224BB。
- 最终生产UI真实数据回放截图：C:/Users/alxanday/AppData/Local/Temp/ylh-manual-track-ui-20260901.png（非已安装侧栏截图）。
- 全片字幕只留系统Temp，未复制进仓库。临时UI服务已关闭，测试页已关闭、视口恢复。

官方参数及计费说明：[Transcript API](https://docs.supadata.ai/get-transcript)。native仅获取已有字幕；lang是偏好语言，计费不代表一次可以返回多条语言轨。
