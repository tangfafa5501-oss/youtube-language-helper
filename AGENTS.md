【启动第一准则】：以用户最新明确的功能描述为验收依据；用户明确要求不查旧记录时，不再追溯历史。经授权的恢复任务执行 `npm run doctor`，以实际构建、行为断言及最新截图共同验收，严禁口头应付。

# 项目交付规则

- 逐句跟读的固定行为：播放一句 → 暂停该句 `endMs - startMs` 的时长 → 自动播放下一句，循环往下；停顿不随播放倍速缩短。最后一句停住。不得改成无限等待或播放键重播本句。
- 两个模式必须分开：`shadowing` 是“逐句跟读”纯停顿循环，不显示录音/听写/音高曲线，不请求麦克风；`practice` 是右侧麦克风“跟读”练习，保留录音、听写、片段范围和音高曲线，不自动跳下一句。两个入口不能共用点击函数或激活状态。
- 逐句跟读由专属按钮/E 独立切换；普通播放/暂停共用按钮和 Space，播放时进入连续播放并取消旧跟读计时器。暂停、切句、录音、断开和换视频不得被旧计时器抢播。
- 恢复任务优先复用已有实现，保留所有未提交改动；已正常则报告无新增生产 Diff，不制造修复。历史只能辅助定位，不能覆盖用户明确修正的行为定义。
- 代码变更必须自动构建并完成扩展端到端验证，不要求用户手动编译、重载或刷新。纯问题解答、只读审查及用户明确禁止写入/构建的任务不因此获得修改授权。
- 不交付 `alert()`、TODO 或模拟成功等功能占位实现。按钮必须绑定真实的 `toggleShadowing` / `playMode`，保留已有 E 键入口及句末暂停控制器。
- `npm run doctor`：只对已知按钮、提示条、enforceBoundary 方法和 timeupdate 绑定进行可重复的局部自愈；先备份，有歧义则失败退出，禁止覆盖整个文件。
- `npm run verify`：构建并在独立浏览器加载真实扩展产物；不得控制用户的日常 Chrome。
- 运行截图必须来自 Playwright/Puppeteer 实际加载的扩展，保存到 `artifacts/verification_latest.png`。本测试使用模拟字幕/API与原生媒体时钟，必须标明 `simulated-test-browser`，不得冒充用户安装环境或真实字幕网络验收。
- 验证记录必须含提交/源码/构建身份、运行环境、时间和数据来源；失败不得沿用旧成功截图。完整记录在 `artifacts/doctor/`，最新截图对应 `verification_latest.json`。
- Git pre-commit 自动运行 doctor；发现修复、未暂存/未跟踪源码、构建或测试失败则拒绝提交。脚本不自动暂存、提交、推送或覆盖用户其他修改。
- 不改原始字幕时间轴，不改无关 UI、Recharts、麦克风、平台取字幕管道。任务结束更新 HANDOFF.md，并明确哪些内容仅通过独立测试浏览器验证。
- 本地 Hook 安装：`git config --local core.hooksPath .githooks`；已有自定义钩子须先保留和合并，不得直接覆盖。
