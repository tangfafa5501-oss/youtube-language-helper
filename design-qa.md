# Design QA — 录音条目与顶部留白（2026-09-04 最新）

## 目标、状态和证据

- 本次目标为用户纠正后的三点录音选择、取消下载/变速/独立录音历史、顶部适量真实空白，以及原声异常不遮住录音曲线。不是复制整个 Enjoy 应用，不新增其音效增强按钮或文件目录功能。
- 视觉源：`C:/Users/alxanday/AppData/Local/Temp/codex-clipboard-081b55b3-f9b9-4e1f-bc97-e906e63c8321.png`，795×1353；用户失败图 `codex-clipboard-6092b78e-9c6a-4c5d-a867-0d3e3ae7ed79.png`，793×1351，同一 Temp 目录。源图含浏览器侧栏外框，DPR/zoom 未知，不虚构1:1密度。
- 最新实现目录 `D:/github/youtube-language-helper/artifacts/doctor/2026-09-04T02-04-55-012Z/`。普通 `recording-menu-{bilibili,youtube}-simulated-test-browser.png` 为430×900 CSS/像素、DPR1；窄屏 `recording-menu-narrow-*-simulated-test-browser.png` 为320×760、DPR1；取消态 `recording-fallback-*-simulated-test-browser.png` 为430×900。
- 已将原图与普通/窄屏/取消态实现放在同一次图像输入中对照；全图中三点菜单、进度条、删除图标和顶部间距均清晰可读，逐项聚焦这些区域，不需要额外失去上下文的裁切。尺寸/文本/音频不同，不用伪造曲线或改变全文字号追求未知缩放下的像素相等。
- 工作树基于 `393c3ce256d922c70111f00ed6f3e6ba2fc6b133`；构建 SHA-256 `230c13723a3b46417b3722e1a0fce9708870d5e1ec4e9f9c327dbb30821e13c6`。测试浏览器采集时间 `2026-09-04T02:05:01.733Z`—`02:06:37.823Z`；Chrome152.0.7977.65 / Windows / Node26.3.0。
- 独立测试浏览器加载实际扩展；仿真字幕/API/WAV和虚拟麦克风，真实原生录制、媒体时间、存储及组件渲染。不是实际站点网络、物理麦克风或 installed-real。

## Findings 与比较历史

| 等级 | 问题与证据 | 修正及后验收 |
| --- | --- | --- |
| P1 已修复 | 用户图中的原生音频三点只显示下载/变速，另有重复历史 | 新独立回放行+编号菜单；最终两平台图显示三份录音，实际选择改变播放 Blob 和橙色曲线，1x、不自动播放 |
| P1 已修复 | 原声错误分支会替代整个图表，即使录音可用 | 错误分源；取消态截图有录音数据和回放，重试后双来源恢复。异步取消竞态另由单元反例与浏览器拖动捕获/修复 |
| P2 已修复 | 01-57-08 图中初版进度条仍是浏览器粗棕条、删除用X，偏离目标控件语义 | 改为细浅轨/白圆点、原始 Lucide 垃圾桶；最终普通/窄屏截图均确认 |
| P2 已修复 | 02-02-34 取消态虽测到32px偏移，但上一句尾部占据留白 | 真正给选中范围第一行加前置32px；02-04-55取消态空隙内无上一句尾巴，并验证前项底边在提示栏下界以内 |
| P2 证据问题已修复 | 缩窄后立刻读菜单位置，可能落在浮层重排中途 | 等实际按钮/菜单边界就绪，再测量及截图；两平台320px无重叠/越界，弹层空间不足时向上展开 |

## 五项视觉核验

1. **字体**：沿用项目系统无衬线层级，菜单12px、时间11px、练习标题14px；序号、时长不挤压，窄屏说明正常换行。源图字号/缩放未知，保留项目一致性，不宣称精确字体复刻。
2. **布局**：黑色圆形回放按钮、进度、时间、最右三点；带编号/时长/删除的浅色浮层。高亮靠上，32px真实空白；范围控制和录音卡仍在高亮之后，首末行有足够滚动空间。320px菜单自动向上是防越界的预期差异。
3. **颜色**：保留浅黄卡片、青色原声/橙色录音和橙色录音按钮；自定义进度浅轨白点，菜单选中项灰底。未修改用户现有主题与整页配色。
4. **资产**：使用实际 Recharts 音频分析图和已有按需 SVG；新增垃圾桶直接来自 [Lucide 0.479.0](https://raw.githubusercontent.com/lucide-icons/lucide/0.479.0/icons/trash-2.svg)，保留现有 ISC 许可。无生成图、品牌冒用、截图背景或图标库全量依赖。
5. **文案**：“录音 #1/#2/#3”、正常时长、播放录音/暂停回放、选择录音/删除录音；没有下载、变速和录音历史入口。原声失败只提示原声错误及“重试原声”，不把失败录音伪装成成功。实际参考没有取消态，取消态以数据独立性要求验收。

## 行为与证据门禁

- 三份录音与存储 Blob 的哈希逐项相等，三个内容不同；切换 paused/0秒/1x，播放时间实际前进。删除未选项不改变选中内容、删除选中项安全回退、重开句子仍保留编号和音频。
- 保留 G 回放、R 录音、E纯停顿、F独立练习、Space松键保持；没有新按键或文件系统权限。
- 18次顶部几何值为31.5—32.28125px，前项不侵入留白；手动滚动不被时间更新拉回。普通/窄屏菜单和底栏均有边界断言。
- 最终133/133单元、90/90集成、156/156实际扩展运行断言；构建和类型检查 Exit0，14条执行哈希记录匹配。0应用错误，36条已保留preload/cross-world警告。原始报告/日志在上述最终目录。

## Implementation Checklist

- [x] 编号菜单对应真实录音，不下载或变速；选中后不自动播放。
- [x] 原声异常与录音曲线分离，取消/重试有自动反例验证。
- [x] 顶部真实留白、首末行与范围、320px布局均验收。
- [x] 原图与最终浏览器图片同输入对照，记录仿真数据和安装态边界。

无剩余本轮范围内可执行的P0/P1/P2问题。P3：参考图DPR/字体回退未知，不作逐像素复刻承诺；物理人声与用户安装态仍是独立验证范围。

final result: passed

---

# 历史 Design QA — 音高说明、F 麦克风提示与居中（2026-09-04）

下方为此前要求居中时的历史记录；当前需求及验收以顶部最新章节为准。

## 目标、状态和证据

- 源图一：`C:/Users/alxanday/AppData/Local/Temp/codex-clipboard-2334c12d-b33a-43f9-a090-b503f6df87bb.png`，750×498 像素，音高帮助悬停、浅黄卡片、原声曲线、橙色录音按钮。
- 源图二：`C:/Users/alxanday/AppData/Local/Temp/codex-clipboard-046a8cc7-23b6-4a8d-87cc-8eec45101053.png`，763×227 像素，底部麦克风开启状态及黑底功能提示。
- 实现截图目录：`D:/github/youtube-language-helper/artifacts/doctor/2026-09-04T01-04-43-973Z/`；`pitch-help-bilibili-simulated-test-browser.png`、`microphone-help-bilibili-simulated-test-browser.png` 与对应 YouTube 文件，430×900 像素，CSS 430×900、DPR 1、未增加页面 zoom。窄屏 `narrow-centered-*-simulated-test-browser.png` 为 320×760、DPR 1。
- 源图是局部截图、DPR/zoom 未提供。没有猜测物理像素到 CSS 的精确比例，按局部控件层级/色彩/文案与响应式边界比较，不给伪精确像素差分。全图含原声/已录音双曲线，源图仅原声，数据和曲线形状不同是预期，不伪造波形。
- 分别将源图与当前 B站实现放在同一次图像输入中检查，再检查 YouTube/320px 画面；不是把分别打开的图片谎称物理拼接对比。音高图实际占 378×100 CSS px、完整卡片 410×420.80，说明字与底栏在 430px 图中均清晰可读，故不另做会移除上下文的裁切。
- 独立 Chrome 152.0.7977.65 实际加载 `.output/chrome-mv3`。字幕/API及变频/变音量 WAV 是测试生成，麦克风为虚拟设备；原生媒体时钟、音频分析、录音和事件真实执行。不是每日浏览器安装态，也不是真实站点网络验收。
- HEAD `393c3ce256d922c70111f00ed6f3e6ba2fc6b133` 的工作树构建；构建集合 SHA-256 `cbdcbf770797515afc7e215f02d6dc8520b3d9c496a272dd5f52ce00a4aa0701`；浏览器采集 `2026-09-04T01:04:50.580Z`—`01:05:48.884Z`。

## Findings 与修复历史

| 等级 | 发现及影响 | 修复和最新证据 |
| --- | --- | --- |
| P1（已修复） | 说明声称阴影是音量，但旧填充是音高/播放进度；仅容器存在也可能全无音高线 | 改用 Line 音高与 RMS Area 音量，播放位置单独竖线；补充静音断段及开关断言。`00-56-57-744Z` 实际轻声漏检失败另由按采样率分帧及检测输入归一化修复；最终线与阴影可见 |
| P2（已修复） | `00-59-33-697Z` 320px 练习底栏把麦克风挤出右侧 | 仅调整 max-width350 的控件宽度/间距，未删 E 按钮或听写入口；最终 320×760 图与按钮逐对无交叠/越界断言通过 |
| P2（证据缺口已修复） | `01-00-52-559Z` 音高帮助图底部被固定底栏挡住，无法目视验证完整图表/录音按钮 | 不据此宣称视觉完成。等待滚轮异步位移后人工查看整卡状态，加入整卡可见断言和截图前后位置记录；最终两平台卡片 y≈255—676，底栏 y=842，无截断 |

最终没有剩余可执行 P0/P1/P2 项。旧失败图不作为本次成功证据；定位记录与成功运行均保留。

## 五项视觉核验

1. **字体/排版**：沿用系统无衬线，图表 High/Mid/Low 10px、说明 12px/1.4、卡片标题 14px/650、录音按钮 14px。用户源图中文字体及缩放未知；没有改掉整个项目字号来追逐未知 DPR。帮助在 320px 可换行且不出屏，12px 说明与既有 UI 层级一致。
2. **布局/间距**：保留浅黄方形卡、问号紧跟“音高曲线”、右侧来源/音量控件、折叠箭头和居中录音按钮。黑色说明圆角8px、8×12px内边距及指向问号的箭头；麦克风提示在底栏上方，避免源截图位于外侧屏幕区域的原生提示被裁掉。
3. **色彩/语义**：原声青色、录音橙色、对应渐变填充/音量虚线；背景沿用浅黄混色，录音 `#f29b20`，已开麦克风灰底，帮助 `#090909`/白字。取消旧灰色进度面，保留独立进度竖线，避免和音量混淆。
4. **图像/资产**：全部为现有按需 Lucide SVG 与实际 Recharts 渲染，没有位图示意图、emoji 替身或生成图片。没有复制 Enjoy 品牌、扩展包或图标代码；测试音频得到的曲线不冒充源截图或真人录音。
5. **文案/内容**：问号完整显示“如何读懂图表”及用户图中文字。麦克风提示按用户最新合同改为“片段播放一次后暂停，按 S 重播；F 关闭”，未复制源图 E/点击播放重播而破坏已确认的 E/Space 逻辑。来源为 [Enjoy 官方音高介绍](https://discuss.enjoy.bot/t/topic/28) 加用户截图，官方文档未说明的细节没有宣称已文档验证。

## 行为与无障碍核验

- F：页面播放器与侧栏切换；完整按下/重复/松开只发一次、不会交给仿真网站全屏；输入框保留 F 文本。E、Space 与录音 R 独立。
- 气泡：悬停及键盘 focus 显示、Escape 关闭、不误触折叠；aria-describedby 与 aria-keyshortcuts，固定气泡按视口自动定位。
- 居中：第三句自动高亮、第一/最后一句、多句范围、收缩范围、320px换行与恢复宽度全覆盖。14 个几何测量最大偏差 0.453125 CSS px；每次高亮改变居中，手动滚动查看卡片不被时间更新拉回。第一句上方留白是用户要求的居中滚动空间，不误作缺失字幕。
- 130/130 单元、90/90 生产包集成、114/114 浏览器检查通过；构建/类型检查 Exit 0。9 个实际执行脚本匹配构建。应用错误0，21条 preload/cross-world警告未隐藏；原始浏览器报告在上述截图目录，集成日志在 `artifacts/doctor/2026-09-04T01-00-52-559Z/integration.log`。

## Implementation Checklist

- [x] 语调曲线与音量填充分开，原声/录音/音量开关有效。
- [x] 问号和麦克风黑色帮助，真实状态及 F/S 提示。
- [x] 逐句停顿、普通播放、录音练习互不混用。
- [x] 320px 持久控制栏与首末行/范围高亮居中。
- [x] 整卡/提示/居中分别有行为断言、原始日志及实际浏览器截图。

Follow-up Polish：P3——未知的源图字体回退及 DPR 差异不作逐像素匹配结论。真实人声和真实网站安装态是另一级证据，未由本报告替代。

final result: passed

---

# 历史 Design QA — 跟读/听写还原

更新时间：2026-09-04 05:18 +08。下方旧报告保留历史含义，不用于当前构建背书。

## 当前目标和证据

- 原版参考：`C:/Users/alxanday/AppData/Local/Temp/codex-clipboard-3e571c80-961c-4d9a-8b3b-c1f8e842323f.png`，550×1219 像素。源截图未提供 DPR；以同尺寸画面及卡片局部核对，不能声称像素级等同。
- 用户指出的失败截图：`codex-clipboard-bbb0fa06-1392-44d9-a9b7-4ec9b17f636a.png` 与 `codex-clipboard-aa888d24-5e35-42f4-8eca-c952a3d70011.png`（同一系统 TEMP 目录）。
- 当前整页：`C:/Users/alxanday/AppData/Local/Temp/ylh-practice-qa-20260904/practice-youtube-real-audio-replayed-captions-test-browser.png`，550×1219，CSS viewport 550×1219、DPR 1、页面 zoom 1.5。
- 当前卡片局部：同目录 `practice-card-real-audio-replayed-captions-test-browser.png`，520×545；参考卡片约 493×535。边框宽度差来自外侧栏/滚动条与视口范围，不以此假称完全同宽。全图和卡片分别与原图在同一图像输入中核对。
- 浏览器：独立 Chrome 152.0.7977.65；官方 CDP 加载真实构建。音频来自 YouTube `BbJrFr2KJAA` 的真实视频，字幕从用户当前浏览器导出后离线回放，时间精度为整秒。不是纯真实字幕 API 通过，不是 installed-real。
- 音频选择是 23—25 秒，因此显示 0:02。参考显示 0:04；未为视觉一致改动项目现有字幕时间及分句层，曲线形状也不应被伪造成参考图片。
- 构建集合 SHA-256：`2a612c1856fe67074d549c95e1c1d322c56bea995d5f143593b3f82808d28522`。未提交工作树，HEAD `dea66688eb14b58e4ba5d900f3109c4c80e7d0ce`。

## 比较历史与修复

| 等级 | 原问题 | 已做修复与后验证 |
| --- | --- | --- |
| P1 | 单字“录/写”替代图标、九列旧网格导致速度与录音入口挤在一起 | 使用原始 Lucide 0.479.0 SVG 资源与 Flexbox 独立按钮；280/320/360/550 CSS px、550px 下 150% zoom 的按钮矩形逐对不相交且位于视口内；截图显示真实麦克风和书本 |
| P1 | 固定 50—500 Hz 纵轴压扁语音曲线，缺少原版幅度/进度语义 | YIN 保持 2048/512 帧/步长；原声 min/max 归一化，录音共用范围；100 点、monotone 面积曲线、默认振幅虚线、播放进度阴影；真实语音实测产生变化和无声断点 |
| P2 | 圆角大卡片、多余百分比横轴与说明占据空间 | 方形浅色卡、100 CSS px 图高、High/Mid/Low、隐藏横轴；操作靠右小图标，历史收起；在真实音频卡片与原图局部比较后通过 |
| P2 | 只检查页面无横向溢出，遗漏按钮相互覆盖 | 新增逐对矩形/点击区域检查；撤回此前“无溢出足以证明布局正确”的结论 |

## 五项视觉核验

- 字体：系统无衬线栈，标题 14px/650，说明 12px，按钮 14px；150% zoom 后与参考的约 21/18px 层级对应，中文字体存在宿主字体回退差异。
- 布局：同色方形卡、左右 16px 内边距、紧凑操作、可折叠图。参考截图裁掉顶栏，而全页验收保留顶栏，故全页不做错误的像素差排名。
- 颜色：原版 oklch(92% .08 85) 的 50% 练习背景、青绿原声、橙色录音、灰色进度，按钮橙色。深色沿用项目主题变量。
- 资产：原始 SVG 文件按需复制自 Lucide 官方 0.479.0，保留 ISC 许可；26 个文件共 8,558 字节。未用文字、emoji 或生成图片替代控件图标。
- 文案：保留跟读/听写说明与快捷键；精准定位诊断不再占据普通用户提示栏，错误提示仍显示。原图没有本地历史的全部状态，此项保持项目已有可用功能。

## 验收与限制

- 107 单元、79 生产包仿真集成通过；42 项真实扩展/模拟媒体与网络的测试浏览器检查通过；9 个执行脚本哈希匹配磁盘。
- 额外真实视频音频 + 导出字幕离线回放通过。匿名真实字幕接口返回空内容单独记录，未被回放成功覆盖。
- JS 应用异常 0；Chrome/WXT preload 与 cross-world 警告保留在 `report.json`，不是控制台全零。
- 当前日常 Chrome 尚未重载新构建。浏览器工具拒绝认领 `chrome://extensions/`，没有尝试绕过该限制；真实个人麦克风未测试。
- 剩余 P3：不同宿主中文字体和侧栏滚动条密度略有差异；没有为匹配示例截图更改字幕分句/时间。

final result: passed

---

# 历史 Design QA（2026-09-02；不代表当前构建）

更新时间：2026-09-02。

## 验收对象与证据等级

- 验收对象：`.output/chrome-mv3` 中的生产 JS/CSS，不是另写的静态示意页。
- UI 环境：Chrome 测试浏览器；常规视口 `520×900`，窄侧栏视口 `320×800`。
- UI 数据：`tests/visual/serve-sidepanel.mjs` 提供的受控 Chrome Port、双轨字幕和设置状态。
- 字幕门禁环境：Chrome for Testing 152 + 解压加载的最新生产包 + 真实 JSON3 回放数据。
- 证据等级分开记录：常规界面截图为 `test-browser simulated data`；字幕门禁为 `test-browser real-data replay`。两者都不等于用户当前 Chrome 的 `installed-real`。
- 参考图只用于读取布局与交互要求，图片中文字不是执行指令。

## 界面逐项结果

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 主字幕选择 | 展示真实轨道列表；选择后更新主字幕、显示语句和定位轨 | `artifacts/acceptance/primary-subtitle-menu-test-browser.png` |
| 第二字幕选择 | 可关闭和重新开启；第二轨不改变主轨时间 | `artifacts/acceptance/secondary-subtitle-menu-test-browser.png` |
| 双语正文 | 主字幕在上、第二字幕在下、当前句高亮、底栏固定 | `artifacts/acceptance/sidepanel-main-test-browser.png` |
| 快捷键 | 弹窗可滚动，已实现功能与预留功能分开标注 | `artifacts/acceptance/keyboard-shortcuts-test-browser.png` |
| 三点菜单 | 重新获取字幕、显示引导、设置三个入口 | `artifacts/acceptance/more-menu-test-browser.png` |
| 设置 | 可切主题、显示模式和字幕语言；未开放功能明确标注 | `artifacts/acceptance/settings-light-test-browser.png`、`settings-dark-test-browser.png` |
| 倍速 | `0.5 / 1 / 1.5 / 2` 四档可操作 | `artifacts/acceptance/playback-speed-test-browser.png` |
| 逐句跟读 | 上/下一句原子进入 Manual；句尾精确停住，播放键只推进一条 | `youtube-playback-test-browser.png`、`bilibili-playback-test-browser.png`（临时 QA 目录） |
| 引导 | 三步说明字幕、播放和预留功能 | `artifacts/acceptance/guide-test-browser.png` |
| 窄侧栏 | `innerWidth=320`、`scrollWidth=305`，底栏控件留在视口内 | `artifacts/acceptance/sidepanel-narrow-test-browser.png` |
| 控制台 | 全流程没有相关 error/warn | Chrome 测试浏览器日志 |

## 字幕 DOM 门禁（上一轮显示专项）

`artifacts/acceptance/test-browser-realdata-replay.png.json` 记录上一轮构建的 24/24 自动断言；它用于保留显示层历史证据，不代表本轮播放构建身份：

- `chrome.runtime.reload()` 已调用，旧 Worker 已关闭，新 Worker 使用不同实例令牌上线。
- 运行时 `background.js`、`content-scripts/youtube.js` 和 `sidepanel.html` 与构建目录 SHA-256 一致。
- `wKpqixrbb6E` 的 JSON3 正文为 237257 字节，SHA-256 为 `441E43D463EBC449BBF4E9BFB70D8F0E19722A034420FAC6455C5DA742896CE0`。
- 最终 DOM：299 行、`data-display-mode=phrases`、所有行都有时间、`underTwoCountFromDom=0`。
- `And what if you were wrong about every single one?` 完整存在；禁止碎片不存在独立行。
- 当时点击目标句后播放器采样约 `10.579s`，采用宽松容差；本轮 20/20 播放门禁已经用 `seeked` 后误差替代该旧判断。

截图：`artifacts/acceptance/test-browser-realdata-replay.png`。

## 播放闭环硬门禁

2026-09-03 的独立 Chrome for Testing 152 验收输出 20/20：

- 运行中的 YouTube/B站内容脚本与当前生产构建 SHA-256 一致。
- YouTube 真实 JSON3 回放：`10.560s → 10.560s`、`14.160s → 14.160s`，定位报告误差 `0.0ms`。
- B站受控官方字幕：`1.500s → 1.500s`，定位报告误差 `0.0ms`。
- 两个平台的上一句、下一句、快速连击、Manual 句尾强制暂停、450ms 不续播及播放键只推进一条全部通过。
- YouTube 原生 CC 状态保持 `true → true`；B站原生 text track 保持 `count=1, mode=showing`。
- B站侧栏断连后旧 Manual 边界不再暂停播放器，证明旧监听器已销毁。

证据等级为 `test-browser`：YouTube 字幕为真实数据离线回放且播放器在线；B站接口数据为受控响应，不是 live API；两者都不是用户当前安装环境。

## 已修复的界面问题

字幕和设置下拉曾因宽泛的 `span:last-child` 选择器压缩文字，Portal 也无法继承设置区主题变量。当前实现已分开约束选项文字与勾选标记，把主题变量提升到根节点，并显式提高下拉层级。常规与窄视口复验均通过。

最终结果：passed。
