以用户最新明确的功能描述为准；明确要求不查旧记录时不要查历史。经授权的恢复/自检任务运行 npm run doctor，保留原有实现与未提交工作，以实际行为断言和最新截图共同验收。
逐句跟读：句末暂停本句 endMs - startMs 的时长，然后自动播放下一句并循环；专属按钮/E 独立切换。普通播放/暂停共用按钮和 Space，按播放进入连续播放并取消旧跟读计时器，不能重播本句或无限等待。
逐句跟读（shadowing）仅负责停顿，不含录音、听写和音高曲线；麦克风跟读（practice）是独立的录音练习模式，不自动跳句。两个入口不得共享点击函数、可见区域或激活状态。
按 AGENTS.md 执行：授权的代码任务自动构建和端到端验证，不向用户转嫁手动编译、重载或刷新。
禁止交付 alert()、TODO 或模拟成功占位符；逐句跟读必须连接真实 toggleShadowing/playMode 和句末暂停。
Playwright 只能使用独立测试浏览器加载扩展产物；不操作日常 Chrome。
artifacts/verification_latest.png 必须来自真实渲染；模拟数据须明确标注，不能当作用户安装环境验收。
保留其他工作树修改；仅已知结构可自动修复，歧义或失败必须非零退出。
