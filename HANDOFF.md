# Video Language Helper 交接

更新时间：2026-09-01。继续前先读本文件、`docs/design.md` 和 `docs/m0-validation.md`。

## 1. 我们在做什么

在 `D:\github\youtube-language-helper` 开发独立 Chrome MV3 字幕学习侧栏，技术栈为 WXT 0.21.4、TypeScript、React 19、SBD 1.0.19。当前支持 YouTube 与 Bilibili：YouTube 使用已确认可用的 Supadata native 标点字幕并与网页自动轨词级时间对齐；B站使用网站 WBI/官方字幕和网页登录态，不调用 Supadata。

产品目标是按用户的朗读停顿规则显示有独立时间的语段，支持点击定位、上一句/下一句、单句播放、500 ms 单句循环、连续播放、倍速和跟读录音。网站有现成英中双语时直接取网站双语，不再做翻译。范围不含 Netflix、语音评分、无字幕转写或商店发布；沉浸式翻译已经关闭，不再调查。

## 2. 完成了什么

- Git 基线已本地提交：`d173564 feat: establish language helper M0`。仓库没有 remote，未推送、未发布。
- YouTube 真实 21 分钟视频 `X627czLUsGY` 已验证前段 `0.040s`、中段 `595.560s`、20 分钟后 `1219.520s` 点击定位；真实单句、循环、连续、0.8×、前后句、快捷键、raw 视图和同视频双标签页隔离通过。
- B站已在官方 Chrome Profile 2 的登录态真实页面加载当前生产包。`BV1XB4y1P7xd` 第 1 P（294s）显示网站 `English(US) + 中文（简体）（网站双语）`，正文英语在上、简体中文在下，无翻译请求。
- B站真实第 1 P：`2:03` 条目起点 `123.680s`；点击后 `123.799s`。单句结束回 `123.680s` 并暂停；循环回起点后读到 `125.069s` 且播放；连续模式越过句尾到 `131.305s`。下一句 `31.443s`、上一句 `28.715s`；0.8× 实际 `playbackRate=0.8`。
- B站真实双标签页：当前页点击 `02:46.010→02:48.850` 后为 `166.143s`；另一标签保持 `61.762s` 且暂停。真实第 1 P→第 2 P→第 1 P 也通过：第 2 P 使用活动 cid `317726202`，点击 `0:51` 到 `51.795s`，另一第 1 P 保持 `16.346s`；切回后列表恢复第 1 P 内容。
- 已修复真实环境暴露的问题：页面桥重复回退；`view` 429；错误选择第一个分 P cid；双语轨选择到夹带原文的中文候选；滚动 cue 重复前缀与十几秒长条。当前优先活动分 P cid 和网站简体中文轨，学习视图按语言分层去重，并仅在真实 cue 边界把多 cue 派生语段限制为 6 秒。原始轨对象、顺序和时间保留。
- 最终检查：52/52 单元检查、27/27 生产 bundle 集成检查、TypeScript 类型检查、生产构建和 `git diff --check` 通过。生产包位于 `.output/chrome-mv3`。
- 真实截图：`docs/evidence/bilibili-production-sidepanel-real.png`；真实语言菜单：`docs/evidence/bilibili-language-menu-real.png`；YouTube 功能截图：`docs/evidence/enjoy-functional-real-screen-passed.png`。

## 3. 卡在哪里

- M0 主流程没有已知代码阻塞；等待用户根据真实截图和行为数据决定是否接受。
- 当前真实 B站分 P 都不足 5 分钟，因此 B站 20 分钟后的轴仍只有受控模拟；整体“至少 20 分钟”真实验收由 YouTube 21 分钟样本完成，不能把 B站模拟写成真实。
- 本地麦克风录音控件已实现，但没有使用用户真实麦克风验收；没有语音评分，符合范围。
- 没有 remote，所以只能确认本地提交，不能声称已推送。

## 4. 下一步计划

1. 用户验收若指出具体字幕或播放问题，只在真实复现后做最小修复；继续区分真实浏览器、真实数据回放和受控模拟。
2. 若以后明确要求 B站 20 分钟以上独立实测，再选有官方 CC 的长视频补证据。
3. 不为后续验收自动接入翻译、转写、语音评分或新供应商。

## 5. 碰到哪些问题

- 最初只凭页面第一个 `[data-cid]` 取分 P；真实第 2 P 页面有 43 个 cid，第一项仍是第 1 P，导致轨道请求错配并显示 `Failed to fetch`。改为优先 `.bpx-state-multi-active-item[data-cid]` 后真实通过。
- 初版页面桥等待 300 ms 就启动隔离层回退，造成同一页面重复请求并放大 429。现在 MAIN world 同步回 `accepted`，隔离层等待完整结果，不再并发回退。
- 匿名 `view` 接口受 429 影响。当前从 BV 号本地换算 aid，并从活动页面元素读 cid；只有页面缺少 cid 才调用 `view`。
- 第一个中文候选“中文（中国）”真实正文夹带大量英文原句，合成后看起来重复。网站另有干净的“中文（简体）”；当前明确优先后者，语言菜单真实显示该选择。
- B站滚动字幕相邻 cue 会重复前缀，原配对算法还可能连续聚合到十几秒。当前只在派生学习视图去掉相邻滚动前缀、分语言排列并限制多 cue 语段；raw 视图仍可审计网站原始内容。
- 浏览器缩放为 150%，早期截图只截到 1707×1067，误判工具栏和侧栏位置。之后启用 DPI aware，证据图为真实 2560×1600 屏幕。

## 6. 如何避免再次出现

- 新会话先读三份交接文档，不再重复排查已确认可见的 Supadata 字幕，不要求重配 Key，不重新选供应商。
- 每次先看真实侧栏正文和播放器数值；“能显示”、构建通过和模拟 Port 都不能代替点击、句尾、循环、切视频或多标签页验收。
- B站分 P 必须取活动项 cid，不能用通用的第一个 `[data-cid]`。同一接口只允许一个在途请求；页面桥确认后不得并发回退。
- 双语轨必须核对实际正文质量，不能只按 `zh-*` 代码或人工/AI 标签决定。展示层修复重复时保留原始 raw，任何切分都必须落在网站真实 cue 边界。
- 记录精确边界：截图证明画面，播放器读数证明行为；YouTube 长视频真实结果与 B站长轴模拟结果分开写。
- 不修改 Chrome 管理文件、不读取其他扩展存储或浏览器凭据；普通刷新、导航、重启和允许的页面操作可以自主完成。
