# YouTube Language Helper 交接

更新时间：2026-09-01。后续开始先读本文件、docs/design.md、docs/m0-validation.md。

## 2026-09-01 Enjoy 功能对齐与 B 站接入（最新）

### 1. 我们在做什么

扩展现在是 YouTube / Bilibili 双站字幕学习侧栏。YouTube 继续使用 Supadata 标点文本 + YouTube 词级时间；B 站复用 `bilibili-digest` 的 MIT WBI/官方字幕链路。界面和播放行为以 Enjoy Echo 0.7.5 安装包及用户截图为参考，分句仍用本项目规则。

### 2. 完成了什么

- Enjoy 实际播放行为已对齐：单句播放到句尾暂停并回起点；单句循环回起点后等待 500 ms 重播；连续播放不拦截句尾。倍速为 Enjoy 的 0.75/0.8/0.9/1 菜单，上一句、下一句、Space、R、当前句跟随均有实现。
- 真实 YouTube 验证语段 `44.360→47.199`：点击到 44.414；单句停在并回 44.360；循环在 44.407 恢复；连续播放到 47.406。0.8×、前后句、空格暂停和 275 条 raw 视图通过。证据图 `docs/evidence/enjoy-functional-real-screen-passed.png`。
- 真实打开快捷键弹层和 API 设置页；设置页有密码输入、保存、账户测试和删除 Key。不引入 Enjoy 的翻译、评分、账号同步和 Netflix。
- B 站新增 `bilibili.content.ts`、WBI 签名与官方字幕解析；优先网站已有的中英双语轨，否则按人工中文/中文 AI/英文选择。保留 `{from,to,content}` 原始字段，单 cue 内无词级时间时不伪造新起点。语言下拉直接切网站轨，不调 Supadata。
- B 站登录态修复：新增 MAIN world `bilibili-main.content.ts`，轨道请求从 B 站网页环境发出并复用网站登录 Cookie；隔离 content script 只接收经过绑定的结果。页面桥不可用时才回退旧请求，避免重复接口调用。
- B 站网站双语修复：接口常将主/副字幕作为独立英语、中文轨返回。现在检测现成英中轨，生成“网站双语”选项，按真实 cue 时间聚合两轨文字；英文在上、中文在下，不请求翻译，不按字符估算时间，原始两轨对象保存在 `raw.primary/raw.secondary`。
- 双语 cue 进入 SBD 后保持英文/中文换行和同一真实时间；快速切轨改为新请求取消旧请求，旧轨不会在稍后覆盖当前选择。侧栏语言下拉优先显示实际 `state.trackId`，B站合成轨明确显示“网站双语”。
- B站生产脚本已分别验证 SPA 切视频旧会话拒绝、同视频双标签页隔离、单句/循环/连续模式；20分02秒双语合成轴验证前段、10分钟和20分钟后真实 cue 边界，原始两轨成员各保留一次。
- MAIN world 返回值经过隔离层严格校验；只接受 B站/hdslb HTTPS 字幕 URL、有限字段和有限轨数，网页伪造外域轨会被拒绝。
- 50/50 单元检查、26/26 生产 bundle 集成检查、TypeScript 类型检查和最终构建通过。生产构建包含三份 MIT license。
- 第 11–13 轮继续验收确认真实 B 站页刷新正常，页面仍显示网站提供的中英双语；给 `.output/chrome-mv3/content-scripts/bilibili.js` 临时追加唯一构建指纹后刷新，页面未出现指纹。这是当前用户 Chrome 尚未重新加载磁盘生产包的直接证据，不再把“可能未加载”写成猜测。临时指纹随后用 `npm run build` 完整重建移除，最终包未保留调试代码。

### 3. 卡在哪里

- Chrome Profile 2 已登记解压扩展 ID `fkmlglnhecdppjdpanlngheoaaagfden`，路径正是 `.output/chrome-mv3`；构建指纹实测证明当前运行时仍未读取磁盘新版。自动控制被浏览器策略禁止进入 `chrome://extensions` 和直接扩展 URL，也明确禁止换接口绕过。普通视频页刷新不能替代扩展重新加载。
- B 站隔离 Chrome 未登录且部分请求触发 HTTP 429。后来重新检查当前普通 Chrome，页面可见个人空间/消息/收藏，确认实际上已登录；旧 `__INITIAL_STATE__` 字段缺失导致此前 `loggedIn=false` 判断错误。真实播放器字幕菜单显示“中文（中国）/中文（简体）/English(US)”与“双语字幕”，画面同时显示中英文，证实网站双语由现成主/副轨组合。截图 `docs/evidence/bilibili-website-bilingual-real.png`。由于当前运行扩展是否已重载到最新生产包仍未知，扩展自身的 B站字幕显示/播放尚不能标为真实通过。
- 本地跟读录音控件已实现，但未用用户真实麦克风验收；没有语音评分，符合排除范围。

### 4. 下一步计划

1. 在已登录 B 站且明确有官方 CC 字幕的视频上重载当前生产包，确认页面桥取得真实主/副轨及“网站双语”标签。
2. 实测 B 站前/中/后点击、单句/循环/连续模式和切分 P/多标签页隔离；不用模拟结果代替。
3. 若真实接口字段与当前受控响应不同，只适配网站实际字段，不自行翻译或伪造时间。

### 5. 碰到哪些问题

- 早期只改截图外观，没有实现 Enjoy 句尾状态机，用户正确判定“功能不符”。
- 第一次真实播放回放时 YouTube 正在播广告，定位被产品正确拒绝；跳过广告后重跑才得到有效证据。
- B 站首次侧栏未连接，根因是 `panel-connection.ts` 只允许 YouTube URL；改为明确允许 YouTube/Bilibili 视频路径后真实连接成功。
- 不能再只用 B站旧 `__INITIAL_STATE__` 判断登录；本次该字段为空但可见账号导航明确存在，播放器也展示真实双语字幕。
- 初版 B站请求在扩展隔离环境中执行，可能丢失网页登录 Cookie；改为 MAIN world 页面桥。初版还把网站双语误认为必须存在单条“双语轨”，现已支持网站主/副轨组合。
- 初版快速切轨用全局 `loading` 丢弃第二次选择；改为取消旧请求。侧栏曾显示首选轨而非实际已加载轨，已改为 `state.trackId` 优先。
- 页面只读接口不能读取 Resource Timing；直接无 Cookie 调用 B 站元数据接口收到 HTTP 429。这两项都不能用于否定已登录网页中的官方字幕，也不能代替扩展侧栏验收。
- Windows 界面控制首次因无法可靠识别前台 Chrome URL而终止；已停止该控制方式。本轮清理空白测试标签后，Chrome 控制接口确认当前选中标签已是已登录 B 站视频，为下一次允许的前台检查消除 URL 不确定性。

### 6. 如何避免再次出现

- Enjoy 对齐必须同时有源码/安装包行为审计、生产桥测试和真实播放边界证据；静态截图只证明外观。
- 所有站点都先验证侧栏绑定的真实 tab/session/track；广告、登录和风控状态单独记录。
- B 站只使用官方字幕时间；无词级时间时不做 cue 内定位，后续若需对齐必须另立真实语音对齐方案。
- 页面桥数据必须在隔离层重新校验，不能信任同页脚本；站点新增功能要单独覆盖快速操作、SPA 和多标签页，不能沿用 YouTube 结果推定。

## 2026-09-01 最新实现与真实画面（覆盖下方历史状态）

- 用户对第一版 Enjoy 风格页回复“不行”。不能把 `docs/evidence/enjoy-standard-real-screen.png` 当作通过。随后补齐参考图的关键行为和密度：播放页每 250ms向当前侧栏发布真实 `currentTime`、暂停状态与倍速；侧栏按该时间自动切换黄色高亮并在离开可见区时跟随滚动。真实隔离 Chrome 中高亮随播放从 `0:19` 自动变为 `0:24`，不是人工改 DOM 或静态选中。新增生产桥集成检查后 18/18 通过。
- 第二版顶部为 `British English` 与可操作的“分句字幕/句子时间组/原始字幕”下拉，并加入键盘、设置图标；字幕字号、行距、条目间距及底部 SVG 播放控制按参考图收紧。分句文字和独立时间没有改回 Enjoy 的 cue 分组，继续使用用户规则。最新真实侧栏截图为 `docs/evidence/enjoy-standard-follow-v2-real-screen.png`；第一版截图只保留为失败历史。
- 已加载字幕页改为 Enjoy Echo 式内容层级：顶部只保留语言、字幕与菜单；中间为无卡片的连续时间戳字幕；当前条目以浅黄色背景和深棕粗体高亮；底部固定上一条、播放/暂停、下一条、重播和倍速。录音按钮未加入，因为本阶段明确排除录音与语音评分。分句和时间仍使用本项目已经确认的 SBD、朗读停顿、≤2 秒向后合并及 YouTube 词级对齐规则，没有复制 Enjoy 实现代码。
- `docs/evidence/enjoy-standard-real-screen.png` 是 Chrome 152 隔离实例加载当前生产扩展、真实 21 分钟 YouTube 视频和此前保存的真实 Supadata en-GB 响应后，由原生 Side Panel 目标直接 `Page.captureScreenshot` 得到的可见区域。画面共有 192 条独立时间语段；实际点击第二条后高亮 `0:04 Today, I am very excited to help you pronounce 100 everyday words`，下一条从 `0:12 in my Modern Received Pronunciation accent.` 开始。侧栏宽 360px、内容宽 345px，无横向溢出。
- 本轮真实 UI 操作已验证第二条点击进入选中状态并发送真实定位；完整前中后、多标签页与切视频证据沿用本节下方既有记录。44/44 单元测试、类型检查和生产构建通过。为自动打开原生 Side Panel 临时加入生成目录的设置页按钮，截图后已重新生产构建删除；源代码与最终包不含该入口。
- 用户复核后补充长句规则：一个较长主句末尾若有内容充足的介词短语，可在介词短语前形成朗读语段；两侧分别至少 10/4 个词，避免拆开普通短语。真实开头第二句现在拆为 `Today, I am very excited to help you pronounce 100 everyday words`（`00:04.880→00:12.240`）和 `in my Modern Received Pronunciation accent.`（`00:12.240→00:16.000`），两段均有独立 YouTube 词级起点。
- `docs/evidence/independent-timestamps-real-screen.png` 是 Chrome 152 测试实例中加载当前生产扩展后，由 Chrome `Page.captureScreenshot` 直接截取的原生 Side Panel 可见区域；不是合成图、静态夹具或打印转图。为自动打开原生 Side Panel 曾在生成目录临时加入设置页测试按钮，截图后已重新生产构建移除，源代码和最终包均不含该按钮。
- 新规则应用于完整真实字幕后由 183 个增加为 192 个独立时间语段。44/44 单元测试、类型检查与生产构建通过。
- 已实现自然语段的独立时间项，不再是原 cue 内的视觉换行或共享时间：Supadata 人工轨标点正文经 SBD 与朗读停顿规则拆分，再与 YouTube 自动轨 JSON3 词级时间做全局文本对齐。每条 `TimedPhrase` 都有独立 `startMs/endMs`，点击以 `phraseId` 定位自身起点；≤2 秒语段继续向后合并。
- 真实 21 分钟视频 `X627czLUsGY`：保存的真实 Supadata en-GB 响应 275 条，在线读取 YouTube 自动轨词级时间后生成 183 个独立时间语段。首段 `00:00.040→00:04.880`，中段 `09:55.560→09:57.680`，末段 `21:00.799→21:03.720`。
- 真实点击定位已验证：前段目标 0.040 秒、约 10 分钟目标 595.560 秒、20 分钟以后目标 1219.520 秒均由生产扩展定位；最后一段会触发 YouTube 临近片尾自动切换，因此后段准确性改用 20:19.520 验证。
- 真实同视频双标签页隔离已验证：两个标签页 session、trackId 独立；A 点击前段后 B 保持在 595 秒以后，未发生跨标签页定位或状态覆盖。模拟集成的 A→B→A 仍只标记为模拟；受控真实 A→B→A 尚未补齐，不能写成真实通过。
- 生产扩展真实侧栏画面已留存：以 `docs/evidence/independent-timestamps-real-screen.png` 为最新直接屏幕截图。旧 `independent-timestamps-rows.png` 是真实侧栏 DOM 的 Skia 打印渲染后转图，只保留为历史证据，不再称为原始屏幕截图。
- 截图发现成功后顶部状态仍停留在“正在读取”；已最小修复为成功时“字幕与独立语段时间已就绪”，失败时“字幕已读取；独立语段时间不可用”。修复后 42/42 单元测试、类型检查和生产构建通过，生产包约 261.85 kB。
- 本轮真实词级时间来自 YouTube 页面；Supadata 使用此前已实际取得的真实响应回放，没有再次消耗服务额度。没有新增依赖、权限、供应商、翻译或 AI 请求。
- 跟读循环和段末暂停仍属于后续目标，当前截图和 M0 证据不代表该功能完成。

以下早期 v1/v2、共享 cue 时间和待实现记录保留为问题历史；与本节冲突时以本节及当前代码为准。

## 不得再次缩减的用户目标与复读规则

- 每次继续本项目，不论是否为新会话，都必须先复读本文件、`docs/design.md`、`docs/m0-validation.md`，再核对用户最近的验收反馈；不能只凭最后一条消息或自动化测试判断范围。
- 产品目标从一开始就包含：按自然语言分句/语段、每段具有时间戳、每段可独立点击定位，以及后续用于跟读的播放控制（循环、段末暂停等）。阶段拆分只表示实施顺序，不能把这些目标删除或宣称用户没有提出。
- 当前最先要补齐的是自然语段的独立时间项及真实点击定位；随后继续完成至少20分钟视频前中后段准确性、切换视频和多标签页隔离，再承接跟读播放控制。
- 视觉换行、共享原cue起点、模拟播放器、构建通过都不能代替“每个自然语段有独立时间并可真实点击”的验收。
- 如果现有字幕数据无法提供语段内部的真实起点，必须明确报告数据缺口和可验证的技术路线；未经说明不得伪造精确时间，也不得悄悄取消时间戳要求。

**最高优先级纠正：v1/v2没有完成用户要的自然语段时间戳。** 用户明确指出，每个按范本拆出的语段应为独立条目、有对应时间戳并可独立定位；代理此前错误实现为原时间组内换行，并擅自以字数阈值近似分段。截图只证明UI出现，不证明需求完成。不要继续以“分行生效”“测试通过”代替这个缺口。

本轮只读核对已有en-GB真实响应：当前算法214个语段中，125个起点恰好对应原cue边界，89个在cue内部；响应只有text/offset/duration/lang，没有逐词时间。例：开头Today所在句与上一句共处首cue，原响应不能给出Today的独立起点。这不是214个语段划分质量已获用户认可，也不是125个起点的音画准确性已实测。要补齐独立时间，需要更细来源时间或文本-音频对齐；不能把共享cue起点、按字数插值伪装成精确语段时间。本轮未改运行代码、未再次请求字幕、未安装新依赖。

前一轮v1记录：用户给出George段落的9行范本，澄清不要空行，随后明确要求修改扩展。已新增默认“语段分行 · 按朗读停顿（无空行）”，顶部标记 **M0 · 语段分行 v1**；不是只修改Markdown。范本在生产页面实际渲染为9个连续语段，无额外行间空隙；长句可在原cue内部显示换行。仍沿用原时间组点击，不提供伪造的每语段时间戳。37/37单元、17/17模拟集成、类型和构建通过；真实275条数据回放为207语段/105时间组。未重载用户安装的扩展，真实播放验收仍未完成。

历史验收反馈：用户在更早交付后明确表示“完全不行”。本次验收按失败记录；下面275条/105组仅为此前真实API响应和本地回放证据，不能证明用户实际扩展可用。已检查当前仍打开X627czLUsGY、本地构建存在、请求语言传递代码存在；尚无当前侧栏截图/错误文字，不能判断是加载、取轨、分组还是定位故障。本轮未改运行代码、未再次调用Supadata、未要求重配Key；等待当前侧栏可见证据，不再盲目重复构建或宣称修复完成。

最新截图与v2修复：用户提供真实侧栏截图，确认带标点正文、连续语段和时间组高亮已在v1生效；不能再说完全没有安装生效证据。截图也暴露破折号插入语被逗号拆散。v2新增破折号边界并重置后续逗号计数，保护连字符词、数字范围和引号内破折号；逗号后each/which/who/where/when/because/although提供额外从句线索，前段至少4词可断。仍为启发式规则，不保证所有语段符合个人朗读习惯。原George范本仍为9行。

当前标记 **M0 · 语段分行 v2**。39/39单元、17/17模拟集成、类型和构建通过；生产Chrome真实275条响应回放显示214语段/105时间组，截图问题句为4个语段，形容词列举不再拆散。模拟点击仍发原cue2，error/warn为空，400px无横向溢出或空语段。真实API调用0次，后台、协议、时间组和CSS不变；v2尚未在用户侧栏验收，真实音画定位仍未验证。截图Temp/ylh-reading-dash-v2.png；测试页/服务收尾关闭、视口恢复。

## 1. 我们在做什么

在 D:/github/youtube-language-helper 开发 YouTube Chrome MV3 扩展，WXT 0.21.4 + TypeScript + React。M0 已有 digest/Supadata native 字幕链路，用户确认能看到字幕；现在修复取错语言偏好导致缺标点的问题，并验收 SBD 分组、点击定位和状态隔离。

用户已批准 sbd@1.0.19 与 ≤2秒向后合并，覆盖早期“不做自然断句”的限制。不做B站、Netflix、AI转录、翻译服务、录音或评分。最新要求：网站有现成双语就直接复用，原文已有中文必须保留；不另找翻译工作。用户已关闭沉浸式翻译，并明确要求停止排查该插件。

## 2. 完成了什么

- 本次只改阅读显示：新增lib/reading-lines.ts及5项回归、调整sidepanel/main.tsx与style.css。既有sbd负责文本句界，补充可调整的逗号/括号规则；不增加NLP库或云调用，不针对George/Mrs.等文字硬编码。默认连续文本，保留原始条目和旧时间组视图；阅读区不再在每组插卡片间隙、时间标题和原始事件详情。所选组的原始事件统一放列表后。
- 规则边界：较长逗号段至少8词才候选切分；后面夹着不超过4词的短插入语时先保留再断；短开头语保留，括号说明单列并携带尾随标点，分号可断。引号内逗号不拆；CJK/双语文本保留已有非空行，不声称英语规则适用于中文。显示层折叠多余空白，原始层不变。这是启发式分行，不是通用语法/语义分析，复杂并列列举仍可能需要调整。
- UI验证为Chrome生产包+模拟Port：范本3条合成时间字幕→9语段，8处行间额外间距全为0；原始模式3条→回语段仍9行，未增加调用。默认窗口和400×900检查通过，无横向溢出或error/warn；窄窗自动折行不等于新增语段。真实275条缓存响应→207语段，末页9语段，最后点击仍发原cue274；模拟换视频清空。截图：C:/Users/alxanday/AppData/Local/Temp/ylh-reading-reference.png、ylh-reading-reference-narrow.png。临时页/服务已关闭、视口已恢复。本轮真实API请求0次。范本截图中的模拟状态沿用旧fixture文字“真实服务数据”，仅为测试夹具标签错误，范本文字及时间是合成测试输入，不是服务返回。
- 最终生产包约247.74kB，目录仍为.output/chrome-mv3。37/37单元、17/17生产模拟集成通过。旧测试数量和旧标记仅作历史记录。

- **已实际取得带标点字幕。** X627czLUsGY 的 en-GB/native/text=false 请求 HTTP200，275条、237条含英文句末标点；SBD输出105组。旧Auto/en请求返回439条且无句末标点。不是Supadata普遍没有标点，也不是必须补标点才能分句。
- 修正此前过度推断：en-GB成功响应仍标 lang=en、availableLangs=[en]，不能只凭这两个字段判定轨道不支持或请求失败。完整对照见 docs/punctuation-source-validation.md。
- 最小取轨修复：lib/transcript-selection.ts 在宽泛en设置下优先网页同语言人工轨en-GB；不全局写死语言，明确区域偏好保留。面板新增“Supadata 请求字幕”，本次覆盖经过后台校验；不修改保存设置、不自动调用、不自动重试换轨。展示请求语言与返回语言。
- 构建新标记：**M0 · SBD · 人工轨优先**。生产目录 D:/github/youtube-language-helper/.output/chrome-mv3，总体约244.71kB。无新增依赖、权限或供应商；当前已有API配置无需重做。
- 32/32单元、17/17生产脚本模拟集成、类型检查与构建通过。测试覆盖人工区域轨优先、显式区域不被替换、非法参数拒绝、返回语言归一、双语原文及换行保留。
- Chrome生产UI使用真实275条响应回放，连接和播放器模拟：105组/2页（100+5），原始275条/3页（100+100+75）；分组/原始切换不发API请求。第1/53/105组点击发送原cueId 0/119/274，对应40/597680/1237400ms。模拟换视频清空旧字幕和播放提示，另一个会话选择恢复默认。400×900无横向溢出、无error/warn。截图在系统Temp ylh-manual-track-ui-20260901.png。
- 历史已修复：无标点SBD尾段不再合全片，按原条目回退后≤2秒合并；句内没有时间不拆原条目。播放前后检查视频会话和发起Port，避免断开/切视频的迟到定位污染其他面板。模拟长片、多标签页、SPA A→B→A已覆盖。
- 用户已亲自确认旧链路字幕可见；不得再要求配置Key或重选供应商。保留每条原文、换行、顺序、offset/duration，点击绑定首原条目。网页直读仍为实验入口。
- 没有提交、推送、发布；仓库文件仍未跟踪，无远程。临时回放不修改用户原视频或已安装扩展。

## 3. 卡在哪里

- **本次语段显示代码及生产UI回放完成，但M0未整体验收通过。** 旧版用户反馈“完全不行”；现在按新范本修改，不把旧测试当新要求验收。尚未证明新构建已在用户安装的侧栏生效，真实播放器误差和实际隔离仍待验收。
- 工具可操作普通Chrome网页，不能读取/点击扩展侧栏，扩展管理页受安全限制。不要绕过，不把刷新网页等同重新加载扩展；不要反复要求用户做代理本来可做的普通页面操作。
- 本视频真实服务响应是英文，尚无真实双语轨获取成功证据。保留双语正文已经测试，但不能写成双语视频实测通过；停止沉浸式翻译相关调查。
- Supadata只按语言偏好取字幕，无已验证的原生轨道ID选择；同语言多轨不能保证精确绑定。也未把YouTube人工轨正文与服务逐字对照。请求en-GB返回en不自动判失败。
- SBD只识别句界，不生成时间；现在文字可在原cue内分行，但同一时间组中各行仍共用组起点。若用户要求每语段独立准确定位，这是尚未解决的时间对齐问题，不能声称本次已完成。非英文分句质量未验收。

## 4. 下一步计划

1. 用户在扩展管理页重新加载现有扩展，确认 **M0 · 语段分行 v2** 和默认语段模式；不重配Key，不重选SBD。普通刷新和允许操作由代理完成，管理页安全限制不绕过。
2. 验收语段是否符合用户朗读习惯、无额外空行；窄侧栏可自然折行。已有字幕切换模式不请求API。当前真实缓存响应为275条/207语段/105时间组，组起点仍为原字幕时间，真实点击偏差另外记录。
3. 补齐真实视频A→B→A、不同视频双标签页、相同视频双标签页、面板断开与迟到请求；发现问题只做最小修复，并分别记录用户实测、代理实测和模拟测试。

## 5. 碰到哪些问题

- 最初把服务en缺标点扩大为“网站无标点”，继而讨论补标点，没有先试网页已存在的区域人工轨；这次en-GB实测已经纠正。Playground没有下拉选项不等于API不支持。
- SBD没有句界也返回剩余文本，旧适配误当完整句，439条变1组；之前只检查文本没丢会漏检，已加组数和长片回归。
- 本轮官方控制台生成代码曾在工具输出包含凭据；未写入项目、夹具或包，也不在交接复制。后续只输出脱敏统计，不再输出生成代码。
- 固定本地测试端口8766本轮报EACCES，改用系统分配空闲端口后成功，非产品错误。一次直接get用户未拥有的浏览器tab失败，之后使用新建受控测试页；不要重复相同错误。
- 真实页面/DOM不包含Chrome侧栏，不能以本地UI回放冒充已安装侧栏验证。独立HTTP200/0字节也不是成功。
- 既有异步播放竞态涉及发起Port生命周期，不能只用视频session判断。多个面板时，不得为取消旧面板操作清空其他面板。

## 6. 如何避免再次出现

- 从用户实际按钮追到最终请求参数及实际响应，先核对原生轨道语言/人工自动属性，再讨论文本算法。native不保证等同浏览器当前所选轨；availableLangs和返回lang不是精确轨道身份。
- 必须分别记录：真实服务响应、真实数据本地处理、模拟Chrome/播放器、已安装扩展和真实音画验收。构建通过不代表已安装生效。
- 原始层不改写/去重/排序，不猜句内时间。不能再用“cue不可拆”阻止纯显示换行；同时不能把显示换行当作新增准确时间。范本必须测试完整9行和无空行；双语原文保留，不自动翻译。
- 保留当前digest流程native/text=false、无chunkSize、最多60次有界轮询和90秒总超时。不重选供应商、不重配Key、不默认generate/auto、不自动追加付费取轨。
- 不读取其他扩展存储或凭据，不修改Chrome管理的包，不绕过浏览器限制。用户已关闭的沉浸式翻译不再调查。
- 普通刷新/导航已有授权，自主完成；无法操作的管理页/侧栏明确说明具体边界，不能停在反复猜测。依赖、权限、破坏性操作或新服务变更仍先说明。未经授权不推送/发布/购买/向他人发送消息。
