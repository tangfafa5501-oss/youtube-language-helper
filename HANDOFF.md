# Video Language Helper 交接

更新时间：2026-09-01。新会话继续前先读本文件、`docs/design.md`、`docs/m0-validation.md` 和 `docs/improvement-ledger.md`。

## 1. 我们在做什么

项目位于 `D:\github\youtube-language-helper`，目标是完成一个接近 Enjoy Echo 基本使用方式的 Chrome MV3 字幕学习侧栏。技术栈为 WXT 0.21.4、TypeScript、React 19、SBD 1.0.19。

当前支持 YouTube 与 B站。YouTube 使用已经确认能显示字幕的 digest/Supadata native 链路，并在网页自动字幕提供词时间时做保守对齐；B站读取网站官方字幕和已有双语轨，不调用翻译服务。核心行为是自然语段时间戳、点击定位、上一句/下一句、单句播放、500 ms 循环、连续播放、倍速、语言/显示下拉、快捷键、API 设置和本地跟读录音。网站有双语就直接取网站双语。

明确不做 Netflix、机器翻译、语音评分、无字幕转写或商店发布。沉浸式翻译插件不在本项目诊断范围内。

## 2. 完成了什么

- `docs/improvement-ledger.md` 保留首批 **100/100**，并单列本轮验收后 101–109 共 9 项实质修正；只统计行为、正确性、稳定性、性能、可访问性或安全性变化，测试、截图、文档和失败尝试没有充数。
- 字幕分段采用 SBD 的句号、问号、感叹号、缩写、数字/货币、URL/邮件/电话、ellipsis/组合标点规则；补充用户范本所需的逗号长从句、冒号、并列连词、破折号、括号和引号停顿。≤2 秒短句向后合并，但不跨越超过 1.5 秒的静音。
- 只有可靠的 Supadata 词级对齐才能为原 cue 内语段产生独立时间；要求首尾词命中、至少 60% 覆盖、顺序与 offset 合法。无法可靠对齐时保留原 cue 时间，不按字符比例伪造时间。
- YouTube 与 B站的播放命令和反馈都绑定 video、session、track；切视频、切分 P、切轨、双标签页、断开面板及迟到请求不能串改状态。单句、循环、连续模式均处理媒体末尾和 `play()` 拒绝。
- B站已经接入网站 WBI/页面桥、活动分 P cid、官方字幕 URL、英中双轨并发读取和语言轨选择。真实已安装扩展曾验证 p1→p2→p1、点击、单句、循环、连续、0.8× 和双标签页隔离；证据保存在 `docs/evidence/`。
- YouTube 真实 21 分钟样本 `X627czLUsGY` 已验证前段 `0.040s`、中段 `595.560s`、20 分钟后 `1219.520s` 的定位，以及播放控制、SPA/轨道变化和同视频双标签页隔离。
- 侧栏已改成 Enjoy 式紧凑时间轴：顶部语言/显示、键盘和三点菜单，下方时间戳双语列表，底部上一句、播放、下一句、模式、倍速、跟读和录音。三点菜单包含“重新获取字幕 / 显示引导 / 设置”；快捷键弹层覆盖 `?/Space/K/Shift+<>/E/H/A/S/D/R/G/Esc`。
- 保存了 Supadata Key 时，YouTube 每个新视频会话只自动提交一次字幕请求；连接后直接显示绿色连接提示和居中的获取进度，不再要求先选语言、点击读取或阅读调试说明。B站继续自动读取网站字幕，不产生 Supadata 调用。
- 修复真实侧栏停在“正在准备字幕”的回归：YouTube 页面桥现在明确发布 `platform: 'youtube'`；侧栏也兼容旧连接状态缺少 `platform` 的情况，并只把明确标记为 `bilibili` 的状态走 B站分支。生产包以缺字段故障样本复测后立即进入“正在获取字幕”，仅产生 1 次 transcript 调用。
- 跟读模式和听写模式已接入底部控制与键盘；连续录制第二遍不会被第一遍录音 URL 的清理 effect 立即停止，迟到的旧 recorder 回调也不能覆盖新录音，切视频会释放麦克风并清除旧录音。
- API 设置页完成保存、账户测试、删除 Key、权限撤销和请求中止。Key 不返回到 UI/日志；损坏或超长 Key 在联网前失败。
- 本轮最终自动化为 **92/92 单元测试、54/54 生产 bundle 集成测试、TypeScript 类型检查、生产构建和 `git diff --check` 全部通过**。生产包为 `.output/chrome-mv3`。
- 最新在应用内浏览器加载真实生产 JS/CSS 做了 420×900 验收：自动加载只有一次请求、无手动读取表单、加载态无溢出；双语时间轴、三点菜单、引导、设置、快捷键弹层和底部控件可操作。点击与 A/S/D 得到 `40→5040→40 ms`，倍速得到 `1→0.9→1`，跟读得到 `single→all→single`。连续两次录音在第二次进行中为 `recordStarts=2 / recordStops=1`，停止后为 `2/2`，两条媒体轨均释放，G 回放一次，Esc 清除播放器；控制台无 error/warn。截图保存在 `docs/evidence/enjoy-*-production-simulated.png`。这仍是“真实生产包 + 模拟 Port/数据”，未冒充已安装新版扩展、真实 Supadata 调用或真实麦克风音频。

## 3. 卡在哪里

- 代码、测试和生产构建当前没有已知阻塞。
- 最新磁盘生产包尚未重新加载进用户已安装的 Chrome 扩展，因此本轮新增行为的浏览器证据是“真实生产包 + 模拟 Port/数据”；之前 `b75bb75` 的 B站与既有 YouTube 结果才是已安装真实网页证据。必须继续保持这两类证据的区分。
- 本地录音生命周期和不支持分支已验证，但没有采集用户真实麦克风音频；项目也不包含语音评分。
- 仓库没有 Git remote，只能本地提交，不能声称已经 push。

## 4. 下一步计划

1. 在允许重新加载最新扩展的真实 Chrome 环境中复核当前生产包的 YouTube/B站点击、单句/循环/连续、切视频/分 P、切轨与双标签页；记录播放器读数，不只看截图。
2. 若真实网页暴露问题，只做能复现该问题的最小修复，并同时补回归测试；继续把真实验证、生产包模拟和纯单元测试分开记录。
3. 用户验收 M0 后再决定 M1；不自动增加翻译、Netflix、语音评分或新的收费字幕供应商。

## 5. 碰到哪些问题

- 页面桥最初在 300 ms 后并发回退，导致重复 API 请求和 429；改为同步确认已受理，失败后只允许显式重试。
- B站同一 BV 的不同分 P 会复用页面元素，第一个 `[data-cid]` 仍可能是第 1 P；现在使用活动分 P cid，并把页号纳入会话绑定。
- Supadata 标点文本与网页词时间来自不同轨时，宽松模糊匹配会产生看似精确但错误的语段时间；现在要求首尾锚点、60% 覆盖和合法词序，失败就保留原 cue。
- 439 条无标点字幕曾被 SBD 视为一个 21 分钟长句；现在对无标点连续区间局部回退到原 cue，并对超大文本设置工作量上限。
- 旧侧栏把说明、统计和调试数据放在正文前，用户难以看到时间戳；现在主体直接是时间轴和播放控制。
- 切视频、切轨和重叠刷新曾可能接受迟到结果；现在所有异步结果与控制都核对 video/session/track 或 BV/page/request generation。
- 普通网页刷新不会让 Chrome 重新载入磁盘上的扩展生产包。此前把“网页已刷新”与“新版扩展已生效”混为一谈，导致验证结论不可靠。
- 侧栏最初把 `button` 也当作输入控件屏蔽，点击过字幕后快捷键全部失效；现在只屏蔽真实输入/媒体控件，字幕行聚焦时 Space/K/A/S/D/E/H/R/G 均生效，工具栏和菜单按钮仍保留 Space 原生激活。
- 新加载态 JSX 最初没有对应 CSS，导致绿色连接提示、卡片、旋转图标和进度条成为裸文字；已补齐样式并验证 420×900 的 `scrollWidth/scrollHeight` 正好为 `420/900`。
- 录音 effect 曾依赖 `recordingUrl`，开始第二次录音时清理旧 URL 会顺带停止新 recorder；现在 URL、媒体流和 recorder 使用独立 ref 与 generation 管理。
- Enjoy 式自动读取最初把 `video.platform === 'youtube'` 当作必要条件，但真实 YouTube 页面桥没有填该可选字段，导致连接成功后永远不触发 Supadata。现在生产者补齐字段，消费者同时保留旧状态兼容；回归样本刻意省略该字段，防止只测新生产者而漏掉已连接的旧状态。

## 6. 如何避免再次出现

- 新会话先读四份项目文档，不再重复排查已确认可见的 Supadata 字幕，不要求重配 API，也不重新选供应商。
- 不根据字符比例、平均时长或下一 cue 猜造句内时间。每个可点击时间戳必须来自原 cue 或经过严格验证的词时间。
- 所有播放命令、反馈、网络结果和 UI 状态都必须携带并核对视频、会话和轨道身份；B站额外核对分 P。
- 真实网页读数证明播放行为，生产包模拟证明界面和协议，单元测试证明边界条件；三者不能互相冒充。
- 每次修改先补能失败的最小回归，再跑完整单元、生产集成、类型检查、生产构建和 `git diff --check`。
- 不改 Chrome 管理文件，不绕过浏览器明确的安全限制；允许的普通页面操作由代理自行完成，并把不可自动完成的真实验收边界如实记录。
