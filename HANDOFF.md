# Video Language Helper 交接

更新时间：2026-09-02。新会话继续前先读本文件、`docs/design.md`、`docs/m0-validation.md`、`design-qa.md` 和 `docs/improvement-ledger.md`。

## 1. 我们在做什么

项目位于 `D:\github\youtube-language-helper`，分支为 `codex/initial-design`，远程为 `origin https://github.com/tangfafa5501-oss/youtube-language-helper.git`。它是一个 WXT 0.21.4、TypeScript、React 19 的 Chrome MV3 字幕学习侧栏：YouTube 使用 Supadata native 字幕并以网站自动轨词时间校准；B站读取网站官方字幕轨。

当前产品目标是复刻用户指定的 Enjoy Echo 侧栏工作流：真实双字幕、自然语段、连续播放、逐句跟读、倍速、快捷键、引导和侧栏设置。听写与录音评分仅保留入口；当前不实现翻译、AI 评分、OCR、无字幕转写、Enjoy 账号或商店发布。

## 2. 完成了什么

- 主字幕和第二字幕完全独立。第二轨只按真实时间映射到主轨行，不改变主轨分句、时间和定位。
- B站优先命名双语单轨；否则默认英语主轨配手工简体中文副轨。YouTube 主轨每个视频会话自动请求一次，第二轨只有用户明确选择时才单独请求。
- SBD 和现有文本规则继续负责标点、语义分句；语音停顿不参与。不足 2 秒才向后合并，6 秒是建议目标，完整自然句允许更长，10 秒为跟读安全上限。
- 当前 UI 只显示连续播放与逐句跟读。逐句跟读在句尾暂停本句时长（2–10 秒），随后自动下一句；等待期间点击播放可立即继续。
- 倍速固定为 `0.5/1/1.5/2`。书本和麦克风按钮点击后明确提示尚未开放，不申请麦克风、不伪装评分。
- 顶部双轨选择、快捷键弹窗、三点菜单、引导、倍速浮层和滑块使用 Radix UI。侧栏设置支持系统/浅色/深色、自然语段/原始字幕、Supadata Key、语言、测试和删除；AI 区只显示未启用。
- 自动化基线为 **113/113 单元测试、60/60 生产 bundle 集成测试、TypeScript 类型检查与 Chrome MV3 生产构建通过**。
- `test-browser` 视觉验收直接加载生产 JS/CSS：520×900 与 320×800 均通过，控制台无 error/warn。11 张证据位于 `artifacts/acceptance/`，逐项结论见 `design-qa.md`。

## 3. 卡在哪里

- 代码、生产构建、自动化与生产包测试浏览器验收都已完成。
- 唯一未升级为通过的是 `installed-real`：浏览器控制策略禁止打开 `chrome://extensions`，且测试接口无法控制用户 Chrome 的原生 Side Panel，所以不能确认用户当前安装实例已经重新加载本文件所在提交。普通网页刷新不会更新已运行的解压扩展。
- 本轮视觉数据为明确标注的受控 B站双轨数据，不是实时 Supadata/B站接口。历史安装版本的真实 YouTube/B站证据不能自动证明当前构建。

## 4. 下一步计划

1. 若以后需要 `installed-real` 证据，只需在可控制扩展管理页与原生 Side Panel 的环境中加载 `.output/chrome-mv3`，无需重做 UI 或测试夹具。
2. 用一个确实返回官方字幕轨的 YouTube/B站视频复核主副字幕切换、逐句跟读和倍速；没有官方轨的视频应明确显示无可读字幕，不做 OCR 猜测。
3. 听写、录音与评分只有在用户另行确认业务流程、供应商和隐私边界后再开发。

## 5. 碰到哪些问题

- 以前把受控生产包截图说得像已安装扩展实测，造成用户看到旧界面时无法对账。本轮把证据固定分为静态、单元、生产集成、真实数据回放、`test-browser` 和 `installed-real`。
- Radix Select 的初版 CSS 用 `span:last-child` 匹配选中标记，误伤了未选中项唯一的文字 span，导致标签被压成 16px。已改为 `span[aria-hidden="true"]` 并在主副菜单复验。
- 第二字幕若复用主字幕状态或同一个请求槽，会改变主轨或互相取消。当前协议、后台任务键和页面状态都按 `primary/secondary` 隔离。
- 逐句跟读若只做定时器而不处理用户点击，会在等待期显得失控。当前等待状态支持播放键立即下一句，切回连续模式也立即恢复。
- 用户参考图包含已实现、要修改和暂缓功能，不能机械照抄。当前只复刻布局与指定行为，未开放功能统一显示为预留。

## 6. 如何避免再次出现

- 新会话先读五份交接与验收文档；需求纠正后回读原目标和最新覆盖规则，不只处理最后一句。
- 报告必须写清问题位置、原因、修改和证据；不用空泛附和。工作超过约 10 分钟时给出目标、事实、证据等级、实质改进、未决项和下一步检查点。
- 截图必须同时说明构建/提交、环境、数据来源、是否真实接口及拍摄时间。`test-browser` 永远不能写成 `installed-real`。
- 先穷尽安全且受支持的自动化方式，再要求用户操作；浏览器原生页面受策略阻止时只记录边界，不绕过限制。
- 原始 cue、时间和主轨始终保持权威；第二字幕、显示语段和 UI 不能反向改写它们。所有异步结果继续核对标签页、视频、会话、轨道和用途。
- 提交前固定执行 `git diff --check`、类型检查、全部单元测试、生产构建集成，并核对 `git status`、remote 与是否实际 push。
