# Video Language Helper 交接

更新时间：2026-09-02。新会话继续前先读本文件、`docs/design.md`、`docs/m0-validation.md` 和 `docs/improvement-ledger.md`。

## 1. 我们在做什么

项目位于 `D:\github\youtube-language-helper`，是一个 Chrome MV3 字幕学习侧栏，技术栈为 WXT 0.21.4、TypeScript、React 19、SBD 1.0.19。YouTube 使用已确认能显示字幕的 digest/Supadata native 链路，并用网站自动字幕词时间校准自然语段；B站读取网站官方字幕及现成双语轨，不调用翻译服务。

当前 M0 范围包括自然语段时间戳、点击定位、上一/下一句、单句/循环/连续播放、倍速、视频/轨道/标签页状态隔离、Enjoy 式菜单与快捷键、本地跟读录音。明确不做 Netflix、机器翻译、语音评分、无字幕转写或商店发布。

## 2. 完成了什么

- 分段架构已改为“文本先定、时间后配”：SBD 固定句号、问号、感叹号及缩写等边界；无标点或过长句再用本地文本语义规则。语音停顿不参与断句。每个完整 SBD 句子独立规划，时间层不能跨句号重组。
- 时间规则为硬性 2 秒下限、6 秒建议目标、10 秒安全上限。不足 2 秒连续向后合并直到达标；与下句隔着长静音时可把当前结束点补到 2 秒。完整自然句可以超过 6 秒。
- YouTube 词轨整体至少 80% token 对齐才采用。一个输出语段只有全部 token 命中才标记 `youtube-word`；局部漏词、错词或重复词使用 `youtube-estimated`，不会让整页分句清空或退回 Supadata 原始块。
- 修复滚动 ASR 事件末词覆盖下一词、精确与估算时间相互重叠、短句合并一次后仍不足 2 秒、开引号被切到上一行、重复示范词没有独立可靠词界等真实数据问题。相邻输出现在不得倒退或重叠；没有可靠独立时间的两行共享一个时间组，不伪造词内起点。
- 真实 21 分钟样本 `X627czLUsGY` 的既有 275 条 Supadata cue 与 2146 个 YouTube 词时间已完成离线联合回放：输出 253 段，173 段精确词时间、80 段局部估算；最短 2.000 秒、最长 8.520 秒，0 倒退、0 重叠。前/中/20分钟后时间轴及用户指出的六个句界全部命中。
- 用户指定结果已覆盖：欢迎句在句号结束；下一段从 `Today, I am` 开始并包含 `100 everyday words`；`everyday vocabulary.`、`at the same time.`、`help you to build fluency` 与前文同段；`first slowly and then faster.` 从 `first` 开始。
- YouTube/B站播放命令和反馈均绑定 video、session、track；切视频、切分 P、切轨、双标签页、断开面板及迟到请求不能串改状态。B站继续只使用网站真实 cue 时间并优先现成英中双语轨。
- 自动化最终为 **109/109 单元测试、56/56 生产 bundle 集成测试、TypeScript 类型检查、生产构建和 `git diff --check` 全部通过**。生产包为 `.output/chrome-mv3`。
- `docs/improvement-ledger.md` 当前为 **116 项**实质改进；测试、构建、截图、文档和失败尝试没有充数。

## 3. 卡在哪里

- 源码、真实数据离线回放和生产 bundle 模拟已通过。本轮磁盘上的 `.output/chrome-mv3` 尚未取得“Chrome 已重新加载该构建并在原生 Side Panel 点击”的新证据；普通网页刷新不会替换正在运行的 content script。
- 历史已安装版本曾实测 YouTube 21 分钟前/中/20分钟后定位、播放模式和双标签页隔离，也实测 B站字幕、双语、分 P 与多标签页；这些证据不能自动证明本轮新分段代码已经被 Chrome 重载。
- 本地录音生命周期和不支持分支已验证，但未采集用户真实麦克风音频；项目不包含语音评分。
- 仓库没有 Git remote，只能本地提交，不能声称已经 push。

## 4. 下一步计划

1. 在 Chrome 扩展管理页重新加载 `D:\github\youtube-language-helper\.output\chrome-mv3`，刷新视频页并重新打开原生 Side Panel。
2. 用 `X627czLUsGY` 抽查本轮六个句界及前/中/20分钟后点击，记录显示起点和播放器定位值；再做一次视频切换与同视频双标签页隔离。
3. 若实机结果与真实数据回放不一致，只修运行环境或具体差异；不再重做 Supadata 选型、Key 配置或 SBD 基础规则。

## 5. 碰到哪些问题

- 最初把 SBD 文本边界和时间优化放在同一个全局规划器里，时间代价会跨句号合并文本；现已按完整 SBD 句子隔离规划。
- Supadata 标点正文与 YouTube 自动词轨不是同一份逐字文本。重复词示范会被 ASR 漏掉或听成其他词；现以 80% 全局门槛和局部估算处理，不把局部失败扩大为整轨失败。
- YouTube 滚动 ASR 事件的最后一个词会继承整事件时长并覆盖下一事件；现只钳制这个不可能的词区间，不把语音停顿当作分段信号。
- 每个句子内部提前补足 2 秒时看不到下一句，曾造成时间覆盖；现统一在所有句子排好后处理 2 秒规则。
- 以“下一 token 起点”切字符串会把下一行开引号留给上一行；现优先使用 SBD/语义规则保存的原字符边界。
- 普通页面刷新不会重载解压扩展。生产 bundle 测试、真实数据回放和已安装扩展实测必须分别标记。
- 历史验收同时存在独立 Chrome 测试实例、生产包模拟页面和用户当前 Chrome 三种环境，旧截图名 `real-screen` 没有标出环境；后续回答又没有先复查历史启动命令，导致对“以前是否能自主验收”作出互相矛盾的解释，并过早把扩展重载操作转交用户。

## 6. 如何避免再次出现

- 新会话先读四份项目文档，不重复排查已经确认可见的 Supadata 字幕，不要求重配 API，也不重新选择字幕供应商。
- 先验证文本：SBD 标点 > 文本语义；再分配时间。语音停顿不作为句界，6 秒不作为强制切点，只有不足 2 秒允许跨完整句界向后合并。
- 每个时间输出必须说明 `youtube-word` 或 `youtube-estimated`；最终检查文本完整、2–10 秒、起点单调、相邻不重叠以及前中后代表段。
- 所有播放命令、反馈、网络结果和 UI 状态必须核对视频、会话和轨道身份；B站额外核对分 P。
- 单元测试证明边界条件，生产 bundle 集成证明打包后的协议，真实数据回放证明实际输入兼容，已安装浏览器证明最终运行环境；四者不能互相冒充。
- 不改 Chrome 管理文件或绕过浏览器安全限制；允许的普通页面操作由代理完成，无法自动触达的原生界面必须如实记录。
- 每约 10 分钟输出一次上下文检查点：目标、已确认事实、证据等级、实质改进、未决问题、下一步。截图统一标注提交、环境和数据来源；以后使用 `simulated`、`test-browser`、`installed-real`，禁止再以含糊的 `real-screen` 作为验收依据。遇到历史能力争议，先从会话日志恢复准确命令，再判断是技术变化、工具变化还是策略变化。
